server:
  port: 8081
  address: 0.0.0.0

spring:
  application:
    name: service-membership
  
  datasource:
    url: jdbc:postgresql://172.31.249.114:5432/membership-db
    username: postgres
    password: SIRIUS
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 2
      minimum-idle: 1

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
