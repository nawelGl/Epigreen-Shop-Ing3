server:
  port: 8081

spring:
  application:
    name: service-membership
  
  datasource:
    url: jdbc:postgresql://172.31.252.28:5432/membership-db
    username: postgres
    password: SIRIUS
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect